# src/shared/

TypeScript types shared between `main` and `renderer` — protocol message
shapes and board-map/pad-identity types. These should be generated from
(or at minimum kept mechanically in sync with) `../../../shared/protocol/`
and `../../../shared/board-map/`, not hand-duplicated.
