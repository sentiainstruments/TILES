# board-map/

Will hold the canonical, versioned 24-pad board map that both firmware and
companion app consume — a superset of
`../../docs/hardware/sentia_tiles_board_map_v1.json` (which is Rev A0's
electrical truth) plus whatever the config/runtime layer needs on top
(default note/scale assignment, calibration slot IDs, per-pad display
names, etc.).

Not authored yet.
